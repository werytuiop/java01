	* 메소드 오버로딩 (Overloading)
	
		＃＃ 한 클래스 내에서 두 개 이상의 이름이 같은 메소드 작성
		
		- 메소드 이름/반환타입 : 같음
		- 매개 변수의 개수/타입 : 다름
			└ 매개 변수의 개수/타입 : 같음, 순서 : 다름  ▶ 허용
			└ 매개 변수의 개수/타입 : 같음, 매개 변수 이름 : 다름  ▶ 허용 안됨
			└ 메소드명, 타입, 매개변수 개수가 완전히 일치  ▶ 허용 안됨
		- 기본적으로 프로그래밍언어에서 이름(메소드,변수)이 같은 경우 같은 기억공간에서는 오버로딩 불가능
			└ (자바에서) 메소드의 이름이 같고 매개변수 유형이 다른 메소드를 허용
		- 리턴 타입은 오버로딩과 관련없음
		- 생성자 오버로딩
	
	
	* 생성자 (Construct)
	
		- 클래스명과 이름이 같은 메소드
		- 반환타입이 무조건 void (void 생략하고 'public 메소드명')
		- 객체를 생성할 때 사용
		- 오버로딩 허용
		- 기본 생성자는 자동으로 생성해줌
		- 다른 생성자를 생성할 시 기본 생성자는 자동으로 생성하지 않음
		- 다른 생성자를 호출할 시 this()로 호출하고 반드시 생성자의 첫번째에 위치
		- 생성자는 객체 생성 시 한 번만 호출